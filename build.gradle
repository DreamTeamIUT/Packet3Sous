buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }
    dependencies {
    }
}

allprojects {
    apply plugin: "idea"

    version = '1.0'
    ext {
        appName = "Packet3Sous"
    }

    repositories {
        mavenLocal()
        mavenCentral()
    }


}

project(":core") {
    apply plugin: "java"

    dependencies {
        compile group: 'org.json', name: 'json', version: '20160810'
    }
}